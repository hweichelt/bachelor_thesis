# Examples

This directory contains a selection of provided examples to apply our unsatisfiable core algorithms to.

## Structure

An example directory has to be structured the following way:

```
example direcory
├── assumptions.lp
├── encoding.lp
└── (extras.lp)(not necessary but optional)
```